/**
 * @eventflow/shared — cross-cutting contracts for EventFlow microservices.
 *
 * Re-exports domain events (envelopes, payloads, catalog) and Kafka utilities
 * (transport, retry, topic config, consumer groups).
 */
export * from './events';
export * from './kafka';
